package com.lhj.scrollerdemo;import android.os.Bundle;import android.support.annotation.NonNull;import android.support.v7.app.AppCompatActivity;import android.support.v7.widget.LinearLayoutManager;import android.support.v7.widget.RecyclerView;import android.view.View;import android.widget.LinearLayout;import android.widget.RelativeLayout;import android.widget.TextView;import android.widget.Toast;import java.util.ArrayList;import java.util.List;import butterknife.BindView;import butterknife.ButterKnife;public class MainActivity extends AppCompatActivity {  //  @BindView(R.id.tex_group_name)    TextView texGroupName; //   @BindView(R.id.rv_name)    RecyclerView rvName; //   @BindView(R.id.rv_data)    RecyclerView rvData; //   @BindView(R.id.scrollview_h)    HorizontalScrollView scrollviewH; //   @BindView(R.id.scrollView_v)    VerticalScrollerView scrollViewV;    private android.widget.HorizontalScrollView mToplayout;;    private VerticalScrollerView scView;    private LinearLayout mTabDataView;    private List<String> mName;    private List<DataBean> mData;    private NameItemAdapter nameItemAdapter;    private DataItemAdapter dataItemAdapter;    private TextView tvTest;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);     //   ButterKnife.bind(this);        rvName = findViewById(R.id.rv_name);        rvData = findViewById(R.id.rv_data);        scrollviewH = findViewById(R.id.scrollview_h);        scrollViewV = findViewById(R.id.scrollView_v);        mToplayout = findViewById(R.id.ll_tabtopview);        mTabDataView = findViewById(R.id.top_data_view);        scView = findViewById(R.id.scView);        tvTest = findViewById(R.id.tvTest);        tvTest.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view) {                scView.smoothScrollBy(0,1000);            }        });        setData();    }    private void setData() {        mName = new ArrayList<>();        mName.add("四创电子");        mName.add("上证指数");        mName.add("路创新头");        mName.add("长安市场");        mName.add("东旭光电");        mName.add("圆通速递");        mName.add("环旭电子");        mName.add("中天科技");        mName.add("辽宁成大");        mName.add("万东医疗");        mName.add("安凯客车");        mName.add("工业副联");        mName.add("国生津空");        mName.add("合兴包装");        mName.add("比亚迪");        mName.add("第一创业");        mName.add("环旭电子");        mName.add("中天科技");        mName.add("辽宁成大");        mName.add("万东医疗");        mName.add("安凯客车");        mName.add("工业副联");        mName.add("国生津空");        mName.add("合兴包装");        mName.add("比亚迪");        mName.add("第一创业");        nameItemAdapter = new NameItemAdapter(mName);        rvName.setAdapter(nameItemAdapter);        rvName.setLayoutManager(new LinearLayoutManager(this));        rvName.setHasFixedSize(true);      rvName.addOnScrollListener(new RecyclerView.OnScrollListener() {          @Override          public void onScrolled(@NonNull RecyclerView recyclerView, int dx, int dy) {              //super.onScrolled(recyclerView, dx, dy);          }      });        mData = new ArrayList<>();        DataBean dataBean= new DataBean("12","0.3%","0.3","1.2","2.1","3772","22","13");        for (int i = 0; i < mName.size(); i++) {            mData.add(dataBean);        }        dataItemAdapter = new DataItemAdapter(mData);        rvData.setAdapter(dataItemAdapter);        rvData.setLayoutManager(new LinearLayoutManager(this));        rvData.setHasFixedSize(true);        rvData.addOnItemTouchListener(new OnItemTouchListener(rvData,rvName) {            @Override            public void onItemClick(RecyclerView.ViewHolder vh, int index) {                Toast.makeText(getBaseContext(), " onItemClick index = " + index, Toast.LENGTH_SHORT).show();            }            @Override            public void onItemLongClick(RecyclerView.ViewHolder vh, int index) {                Toast.makeText(getBaseContext(), " onItemLongClick index = " + index, Toast.LENGTH_SHORT).show();            }        });        scrollViewV.setSVYOnScrollListener(new VerticalScrollerView.SVYOnScrollListener() {            @Override            public void onScroll(int scrolly) {                int height = scrollViewV.getTop();                if(scrolly > 0 && scrolly >= height){                 mToplayout.setVisibility(View.VISIBLE);                }else{                    mToplayout.setVisibility(View.GONE);                }            }        });        scrollviewH.setSVHOnScrollListener(new HorizontalScrollView.SHXOnScrollListener() {            @Override            public void onScrollX(int scrollX) {                mTabDataView.setTranslationX(-scrollX);            }        });    }}